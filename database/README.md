# Database

This directory contains database-related resources for WildSight.

## Structure

- `init/` – Initialization scripts executed when the database is first created.
- `backups/` – Database backup files (if used).
- `migrations/` – Database migration scripts (future).

The application uses PostgreSQL with the PostGIS extension.